# Azkaban - Test Environment Variables
# Used for running tests

# Environment
ENVIRONMENT=testing

# Firebase Configuration (can be mock values for tests)
FIREBASE_PROJECT_ID=test-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nTest private key\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=test@test-project.iam.gserviceaccount.com

# Database (test database)
DATABASE_URL=postgresql://azkaban:azkaban_dev@localhost:5432/azkaban_test_db

# SMTP Configuration (not needed for tests)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=

# Fixed OTP Code (for tests)
FIXED_OTP_CODE=123456
